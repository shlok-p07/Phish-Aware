services:
  ml-service:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      ENVIRONMENT: development
    volumes:
      - ./models_store:/app/models_store
      - ./data:/app/data
    ports:
      - "8001:8001"
